const config = {
  trello: {
    apiKey: import.meta.env.VITE_TRELLO_API_KEY,
    token: import.meta.env.VITE_TRELLO_TOKEN,
    apiUrl: import.meta.env.VITE_TRELLO_API_URL,
  },
}

export default config